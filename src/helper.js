export default function units() {

    // create 10x12 board, fill with null
    var board = new Array(10).fill(null);
    for(var row = 0; row < 10; row++) {
        board[col] = new Array(12).fill(null);
    }
}

// export function placeUnit(board, row, col) {
//     board[row][col] = 
// }