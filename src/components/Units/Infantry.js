import React from 'react';

export default class Infantry extends React.Component {
    render() {
        // let className="unit infantry";
        // if (this.props.isSelected) {
        //     className += " selected";
        // }

        return (
            <div
                className={"unit infantry"}
            >
                IN
            </div>
        );
    }
}