import React from 'react';

export default class Drone extends React.Component {
    render() {
        // let className="unit infantry";
        // if (this.props.isSelected) {
        //     className += " selected";
        // }

        return (
            <div
                className={"unit drone"}
            >
                DR
            </div>
        );
    }
}